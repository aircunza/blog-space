{
  "compilerOptions": {
    "target": "es2016",                                 /* Defines the output JavaScript version */
    "module": "commonjs",                               /* Defines the module system (common in Node.js) */
    "esModuleInterop": true,                            /* Allows interoperability with ES modules */
    "forceConsistentCasingInFileNames": true,           /* Ensures consistent casing of file names */
    "strict": true,                                     /* Enables all strict type-checking options */
    "skipLibCheck": true,                               /* Skips type checking of .d.ts files */
    "outDir": "./dist",                                 /* Output directory for compiled files */
    "types": ["jest"],                                  /* Includes Jest types for testing */
    "moduleResolution": "node",                         /* Uses Node.js module resolution strategy */
    "resolveJsonModule": true,                          /* Allows importing JSON files */
    "baseUrl": "./"                                     /* Base directory for module resolution */
  },
  "include": [
    "src/**/*.ts",                                      /* Includes all .ts files inside src */
    "tests/**/*.ts"                                     /* Includes all .ts files inside tests */
  ],
  "exclude": [
    "node_modules",                                     /* Excludes the node_modules directory */
    "dist"                                              /* Excludes the dist directory */
  ]
}
